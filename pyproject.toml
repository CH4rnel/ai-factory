[project]
name = "ai-factory"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
core = [
    "pydantic>=2.9",
    "structlog>=24.4",
    "tenacity>=9.0",
    "typer>=0.15",
]
dev = [
    "mypy>=1.13",
    "pre-commit>=4.0",
    "ruff>=0.8",
]
test = [
    "hypothesis>=6.115",
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
]
